# 💱 通貨変換機能の技術説明

## 概要

このアプリでは、異なる通貨建ての株式（米国株・日本株）を統一通貨で比較できる通貨変換機能を実装しています。

## 実装の詳細

### 1. 為替レートデータの取得

```python
def get_exchange_rate(base_currency, target_currency, start_date, end_date):
    """
    Yahoo Financeから為替レートの過去データを取得
    例: USD → JPY の場合、USDJPY=X のシンボルでデータ取得
    """
```

**特徴:**
- Yahoo Financeの為替ペアデータ（例: USDJPY=X）を使用
- 指定期間の終値ベースの為替レートを取得
- `@st.cache_data`でキャッシュし、同じデータの再取得を防止

### 2. 株価データの通貨変換

```python
def convert_currency(df, from_currency, to_currency, start_date, end_date):
    """
    株価DataFrameを指定通貨に変換
    各日付の為替レートを使用して、Open/High/Low/Close を変換
    """
```

**処理フロー:**
1. 為替レートデータを取得
2. 株価データのインデックス（日付）に合わせて為替レートを前方補完（`method='ffill'`）
3. 各価格カラム（Open, High, Low, Close）を為替レートで乗算

**注意点:**
- 為替データが欠損している日は、直前の為替レートを使用（前方補完）
- 出来高（Volume）は変換しない

### 3. 銘柄の基本通貨の判定

```python
def get_stock_currency(ticker):
    """
    銘柄の基本通貨を自動判定
    """
```

**判定ロジック:**
- yfinanceの`info['currency']`から取得
- 失敗した場合、シンボルから推測（.T で終わる場合はJPY）
- デフォルトはUSD

## データフロー

```
1. ユーザーが銘柄入力: AAPL, 7203.T
2. 表示通貨選択: JPY

3. 各銘柄のデータ取得:
   - AAPL: 基本通貨 = USD
   - 7203.T: 基本通貨 = JPY

4. 通貨変換処理:
   - AAPL: USD → JPY 変換が必要
     → USDJPY=X の為替データ取得
     → 各日の株価 × 為替レート
   - 7203.T: JPY → JPY 変換不要
     → そのまま使用

5. 統一通貨（JPY）で表示
```

## 使用例

### 例1: 日米テック株の比較（円建て）

```python
銘柄: AAPL, MSFT, 6758.T (ソニー)
表示通貨: JPY

結果:
- AAPL: $180 × 150円/$ = ¥27,000
- MSFT: $370 × 150円/$ = ¥55,500
- ソニー: ¥13,500 （変換不要）
```

### 例2: 日本株の米ドル表示

```python
銘柄: 7203.T (トヨタ), 6758.T (ソニー)
表示通貨: USD

結果:
- トヨタ: ¥3,000 ÷ 150円/$ = $20
- ソニー: ¥13,500 ÷ 150円/$ = $90
```

## キャッシュ戦略

```python
@st.cache_data(ttl=3600)  # 1時間キャッシュ
def get_exchange_rate(...):
```

**理由:**
- 為替データは頻繁に変更されない
- API呼び出しを削減し、パフォーマンス向上
- 1時間ごとに自動更新

## 制限事項

1. **対応通貨ペア**: 現在はUSD/JPYのみ
2. **為替レート**: 日次の終値のみ（リアルタイムではない）
3. **データ欠損**: 休日や祝日は前営業日の為替レートを使用
4. **時差考慮**: 日米の取引時間の違いは考慮していない

## 今後の拡張可能性

### 対応可能な追加機能:

1. **複数通貨ペア対応**
   ```python
   # EUR, GBP, CNY などの追加
   currency_pairs = {
       ('USD', 'JPY'): 'USDJPY=X',
       ('EUR', 'JPY'): 'EURJPY=X',
       ('GBP', 'JPY'): 'GBPJPY=X',
   }
   ```

2. **リアルタイム為替レート**
   ```python
   # 最新の為替レートAPI統合
   current_rate = get_realtime_rate('USD', 'JPY')
   ```

3. **為替手数料の考慮**
   ```python
   # 実際の両替手数料を反映
   converted_price = price * rate * (1 - commission_rate)
   ```

4. **為替チャート表示**
   - USD/JPYの推移を独立したタブで表示
   - 株価と為替の相関分析

## コード品質

- **型ヒント**: 全ての関数にdocstringと型情報
- **エラーハンドリング**: try-except で例外をキャッチ
- **ユーザーフィードバック**: st.warning()で問題を通知
- **キャッシュ**: パフォーマンス最適化

## まとめ

この通貨変換機能により、ユーザーは：
- 日本株と米国株を同じ土俵で比較
- 自分の資産を統一通貨で評価
- 為替の影響を考慮した正確な分析

が可能になります！
